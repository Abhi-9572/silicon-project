import React from 'react'

const CaraousalTest = () => {
  return (
    <div className="swiper-pagination position-relative bottom-0 mt-2 swiper-pagination-clickable swiper-pagination-bullets swiper-pagination-horizontal"><span className="swiper-pagination-bullet swiper-pagination-bullet-active" tabIndex={0} role="button" aria-label="Go to slide 1" aria-current="true" /><span className="swiper-pagination-bullet" tabIndex={0} role="button" aria-label="Go to slide 2" /></div>
  )
}

export default CaraousalTest